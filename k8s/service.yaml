apiVersion: v1
kind: Service
metadata:
  name: oota-agent
  labels:
    app: oota-agent
spec:
  selector:
    app: oota-agent
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
